```ts
const userTable = new Table(this, "userTable");

const getUser = new Function(this, "getUser", async (userId: string) => {
  return $AWS.DynamoDB.GetItem({
    Table: userTable,
    Key: {
      id: {
        S: userId,
      },
    },
  });
});
```
