{
  "name": "functionless-monorepo",
  "version": "0.0.0",
  "private": true,
  "repository": "git@github.com:functionless/functionless",
  "author": "Functionless Team",
  "license": "Apache-2.0",
  "scripts": {
    "build:website": "turbo run build --filter=website",
    "synth": "turbo run synth",
    "typecheck": "turbo run typecheck",
    "prepare": "husky install",
    "prepack": "turbo run compile"
  },
  "workspaces": {
    "packages": [
      "apps/**/*",
      "packages/**/*"
    ],
    "nohoist": [
      "**/@types/jest"
    ]
  },
  "devDependencies": {
    "husky": "^8.0.1",
    "lerna": "^5.5.4",
    "lint-staged": "^13.0.3",
    "turbo": "^1.5.5"
  }
}
