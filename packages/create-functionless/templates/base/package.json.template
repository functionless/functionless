{
  "name": "{{projectName}}",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy": "cdk deploy",
    "synth": "cdk synth",
    "test": "jest",
    "validate": "functionless",
    "watch": "tsc --build --watch"
  },
  "devDependencies": {},
  "jest": {
    "transform": {
      "^.+\\.(t|j)sx?$": [
        "functionless/jest",
        {}
      ]
    }
  }
}
