import { IRuleTarget } from "aws-cdk-lib/aws-events";
import { FunctionDecl } from "../..";

export const synthesizeEventBridgeTarget = (
  _decl: FunctionDecl
): IRuleTarget => {
  throw Error("Not implemented yet.");
};
